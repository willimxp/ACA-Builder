{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "debugpy",
            "request": "launch",
            "name": "Blender: Start",
            "program": "${command:blender.start}"
        }
    ]
}